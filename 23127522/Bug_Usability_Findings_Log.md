# Bug & Usability Findings Log (Scenario C)

**Student MSSV:** 23127522
**Google Form:** https://forms.gle/CJQFQCAXcsDbXDMM9 (submit each finding with your MSSV email)

> Every finding from Task 1–3 must be reported **twice**: once via the Google Form, once aggregated here. This file must match the form submissions — TA may cross-check counts.

| ID | Scenario/Screen | Type (Bug \| Usability) | Description | Repro steps / Heuristic | Severity | Suggested fix | Screenshot ref | Form submit time |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 001 | C — C1 (Users list) | Bug | Avatar trong hàng bảng bị bóp méo từ hình tròn thành ê-líp khi tên/nội dung của user rất dài; text có wrap nhưng phá vỡ layout dòng | Repro: mở C1 Users → nhập/tạo user có tên rất dài → xem hàng đó trong bảng. Heuristic: N1 (Visibility of system status), No1 (Visibility), N8 (Aesthetic & minimalist design) | 2 | Thêm `flex-shrink: 0` + `min-width` cố định cho avatar; cho ô tên `min-width` + `truncate`/ellipsis (hoặc giới hạn ký tự) để không đẩy layout | bugs/C01_IA01-15.png | 01:22 04/08/2026 |
| 002 | C — C1 (Users list) | Usability | Các nút Add User / Export / Edit / Delete không đổi con trỏ thành bàn tay khi hover (thiếu `cursor: pointer`); chỉ nút phân trang có — affordance không nhất quán | Repro: mở C1 Users → rê chuột qua Add User / Export / Edit / Delete (con trỏ vẫn là mũi tên) so với nút phân trang (thành bàn tay). Heuristic: No1 (Visibility/Affordance), N6 (Recognition rather than recall) | 1 | Thêm `cursor: pointer` cho tất cả nút/icon bấm được để đồng bộ affordance | bugs/C01_IA03-09.png | (điền sau khi submit form) |
| 003 | C — C1 (Users list) | Bug | Sau khi tạo mới user thành công, hệ thống không hiển thị toast/thông báo xác nhận — người dùng không biết thao tác đã thành công hay thất bại | Repro: mở C1 Users → Add User → điền hợp lệ → Submit → user được tạo nhưng không có toast/feedback nào. Heuristic: N1 (Visibility of system status), No1 (Visibility), S3 (Offer informative feedback) | 3 | Hiển thị toast "Tạo user thành công" (và toast lỗi khi thất bại) sau khi submit; cho toast tồn tại đủ lâu để đọc hoặc cho đóng chủ động | (không có) | 15:41 04/08/2026 |
| 004 | C — C2 (modal Chỉnh sửa người dùng) | Bug | Khi mất mạng lúc bấm "Lưu thay đổi", form hiển thị lỗi thô "Failed to fetch" — không phải ngôn ngữ người dùng, không nêu lý do (mất kết nối), không có nút Thử lại | Repro: mở form Chỉnh sửa người dùng → DevTools → Network → Offline → bấm "Lưu thay đổi" → form hiện "Failed to fetch". Heuristic: N9 (Help users recognize/diagnose/recover from errors), N5 (Error prevention), S3 (Offer informative feedback) | 2 | Bắt lỗi mạng/timeout/5xx và hiển thị thông điệp dễ hiểu ("Mất kết nối, vui lòng thử lại") kèm nút Thử lại; không lộ lỗi kỹ thuật thô | bugs/C01_IA04-10.png | 15:57 04/08/2026 |

| 005 | C — C2 (modal Chỉnh sửa người dùng) | Bug | Nhập nội dung rất dài vào field trong modal thì nội dung bị tràn, không truncate/wrap gọn trong khung modal | Repro: mở C1 Users → Edit một user → nhập chuỗi rất dài vào field (Tên/Họ/Mã thành viên) → nội dung tràn khỏi vùng hiển thị. Heuristic: N1 (Visibility of system status), No1 (Visibility), S8 (Reduce short-term memory load), N8 (Aesthetic & minimalist design) | 2 | Thêm `overflow-wrap`/`text-overflow: ellipsis` + `max-width` cho field và vùng hiển thị giá trị; giới hạn maxlength hợp lý cho các trường tên/mã | bugs/C02_IA01-15.png | 17:54 04/08/2026 |

| 006 | C — C2 (modal Chỉnh sửa người dùng) | Usability | 3 trường bắt buộc (First Name, Last Name, Email) không có dấu `*` hay chữ "bắt buộc" — người dùng chỉ biết mình thiếu gì sau khi bấm Save | Repro: mở C1 Users → Edit user → quan sát nhãn các field. DevTools xác nhận 3 field có thuộc tính `required` nhưng không có marker thị giác nào. Heuristic: N1 (Visibility of system status), No3 (Constraints), N5 (Error prevention) | 2 | Thêm dấu `*` (kèm chú thích "* bắt buộc") vào nhãn của các trường required | bugs/C02_IA02-02.png | (điền sau khi submit form) |
| 007 | C — C2 (modal Chỉnh sửa người dùng) | Bug | Đang nhập dở mà bấm X hoặc click backdrop thì modal đóng thẳng, mất toàn bộ dữ liệu đã nhập, không có hộp thoại xác nhận | Repro: mở C1 Users → Edit user → sửa nội dung 1 field, chưa bấm Save (ảnh state 1: `C02_IA02-10-CHANGED_NOT_SAVED.png`) → bấm X hoặc click vùng tối ngoài modal → modal đóng, thay đổi mất không cảnh báo (ảnh state 2: `C02_IA02-10-EXIT.png`). Heuristic: N3 (User control and freedom), N5 (Error prevention), S6 (Permit easy reversal of actions), S7 (Keep users in control) | 3 | Theo dõi trạng thái dirty của form; khi có thay đổi chưa lưu thì hiện dialog xác nhận ("Bạn có thay đổi chưa lưu — thoát và bỏ thay đổi?") trước khi đóng, hoặc chặn đóng bằng backdrop click | bugs/C02_IA02-10-CHANGED_NOT_SAVED.png + bugs/C02_IA02-10-EXIT.png | (điền sau khi submit form) |

| 008 | C — C2 (modal Chỉnh sửa người dùng) | Bug | Placeholder của 2 ô tên bị hoán đổi: ô có nhãn "First Name" gợi ý "Last Name", ô nhãn "Last Name" gợi ý "First Name" — người dùng dễ điền ngược họ/tên | Repro: mở C1 Users → Edit user → xóa trống 2 ô First Name và Last Name để thấy placeholder. DevTools xác nhận: field aria-label="First Name" có placeholder="Last Name" và ngược lại. Heuristic: N2 (Match between system and the real world), N5 (Error prevention), No4 (Mapping) | 2 | Sửa lại placeholder cho khớp nhãn của từng ô (hoặc bỏ placeholder vì đã có label rõ) | (chụp bổ sung) | (điền sau khi submit form) |
| 009 | C — C2 (modal Chỉnh sửa người dùng) | Bug | Modal có `role="dialog"` và `aria-modal="true"` nhưng KHÔNG có accessible name (thiếu `aria-label`/`aria-labelledby`) — screen reader chỉ đọc "dialog" mà không biết là dialog gì, dù tiêu đề "Chỉnh sửa người dùng" hiển thị bằng mắt | Repro: mở modal Chỉnh sửa người dùng → DevTools Console kiểm `aria-label`/`aria-labelledby` của phần tử `[role=dialog]` → cả hai đều null. Heuristic: WCAG 2.1 AA 4.1.2 (Name, Role, Value), N1 (Visibility of system status) | 2 | Gắn `aria-labelledby` trỏ tới id của tiêu đề modal (hoặc thêm `aria-label="Chỉnh sửa người dùng"`) để screen reader đọc đúng tên dialog | (không có — lỗi thuộc tính DOM, xem repro) | (điền sau khi submit form) |
| 010 | C — C2 (modal Chỉnh sửa người dùng) | Bug | Modal thiếu focus trap: Tab qua nút cuối cùng trong modal thì focus nhảy ra sidebar/bảng phía sau thay vì quay về đầu modal — người dùng bàn phím "lọt" ra nền bị che, không biết focus đang ở đâu và có thể kích hoạt nút bị modal che | Repro: mở C1 Users → Edit user → đặt focus vào nút "Lưu thay đổi" (nút cuối) → nhấn Tab → focus nhảy vào sidebar phía sau. DevTools Console xác nhận: nền sau không có `inert`/`aria-hidden`, còn 37 phần tử focus được nằm ngoài modal vẫn phơi bày | 3 | Cài focus trap: vòng Tab/Shift+Tab trong modal (từ phần tử cuối quay về đầu), gắn `inert` (hoặc `aria-hidden="true"`) cho nội dung phía sau khi modal mở, và trả focus về nút "Edit" đã mở modal khi đóng. Heuristic: WCAG 2.1 AA 2.4.3 (Focus Order), S2 (Seek universal usability), N3 (User control and freedom) | (không có — lỗi hành vi focus, xem repro) | (điền sau khi submit form) |

| 011 | C — C4 (luồng Export to Excel) | Usability | Bấm Export thì file tải xuống ngay nhưng app KHÔNG hiện toast/thông báo nào báo đã xuất xong — người dùng chỉ biết nhờ thanh download của trình duyệt (có thể bị ẩn/tắt), không phải phản hồi của hệ thống; cũng không có progress bar dù dữ liệu có thể lớn | Repro: mở C1 Users → bấm Export → file `.xlsx` tải về nhưng không có toast, không có dialog, không có progress. Heuristic: N1 (Visibility of system status), No1 (Visibility), S3 (Offer informative feedback) | 2 | Hiện toast "Đã xuất N người dùng ra Excel" khi hoàn tất (và toast lỗi khi thất bại); nếu dữ liệu lớn thì thêm chỉ báo đang xử lý trên nút Export (spinner/disable) | bugs/C04_IA04-01.png | (điền sau khi submit form) |
| 012 | C — C4 (luồng Export to Excel) | Bug | Khi mất mạng, bấm Export thất bại HOÀN TOÀN IM LẶNG: không có file tải về và cũng không có bất kỳ thông báo lỗi nào — người dùng không biết export đã lỗi hay đang chạy, không có gợi ý thử lại. Nặng hơn lỗi ở C2 (ít nhất C2 còn hiện "Failed to fetch") | Repro: mở C1 Users → DevTools → Network → Offline (KHÔNG F5) → bấm Export → không có file, không có thông báo, nút Export trở về trạng thái bình thường như chưa từng bấm. Heuristic: N1 (Visibility of system status), N9 (Help users recognize/diagnose/recover from errors), S3 (Offer informative feedback) | 3 | Bắt lỗi mạng/timeout/5xx của luồng export và hiện thông điệp dễ hiểu ("Không xuất được file do mất kết nối — thử lại") kèm nút Thử lại; trong lúc chờ thì disable nút Export và hiện trạng thái đang xử lý | bugs/C04_IA04-10.png | (điền sau khi submit form) |
| 013 | C — C4 (luồng Export to Excel) | Bug | File `.xlsx` xuất ra không chứa toàn bộ người dùng theo dữ liệu đang hiển thị/lọc trên màn hình — tập dữ liệu trong file không khớp với những gì người dùng thấy, dẫn tới báo cáo thiếu dữ liệu mà không có cảnh báo nào | Repro: mở C1 Users → ghi lại tổng số user hiển thị/đang lọc → bấm Export → mở file `.xlsx` → số dòng user trong file ít hơn số user trên màn hình. Heuristic: N1 (Visibility of system status), N2 (Match between system and the real world), No4 (Mapping), S3 (Offer informative feedback) | 3 | Export đúng toàn bộ tập dữ liệu đang lọc (không giới hạn theo trang hiện tại); nếu chủ ý chỉ xuất trang hiện tại thì phải nêu rõ phạm vi trên nút/dialog ("Xuất trang này" vs "Xuất tất cả") và hiện số dòng đã xuất | bugs/C04_IA04-11.png | (điền sau khi submit form) |

**Severity scale:** 0 = none · 1 = cosmetic · 2 = minor · 3 = major · 4 = catastrophe.
