# usability-session-processor

Use this skill to process one real participant session.

Rules:

- process one real participant session at a time
- calculate only from supplied values
- never fabricate quotes, timings, scores, or responses
- keep masked participant identifiers only
- separate raw notes from synthesized observations
- require human confirmation when source notes are incomplete
